name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: دریافت کد پروژه
        uses: actions/checkout@v7

      - name: نصب Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: پیدا کردن پروژه Android
        shell: bash
        run: |
          GRADLEW_PATH="$(find "$GITHUB_WORKSPACE" -type f -name gradlew | head -n 1)"

          if [ -z "$GRADLEW_PATH" ]; then
            echo "::error::فایل gradlew پیدا نشد. احتمالاً پروژه را به‌صورت ZIP یا ناقص آپلود کرده‌اید."
            echo "فایل‌های موجود در Repository:"
            find "$GITHUB_WORKSPACE" -maxdepth 4 -type f | sort | head -n 200
            exit 1
          fi

          PROJECT_DIR="$(dirname "$GRADLEW_PATH")"

          echo "PROJECT_DIR=$PROJECT_DIR" >> "$GITHUB_ENV"
          echo "Android project directory: $PROJECT_DIR"

      - name: ساخت APK آزمایشی
        shell: bash
        run: |
          cd "$PROJECT_DIR"
          chmod +x gradlew
          ./gradlew assembleDebug --stacktrace --no-daemon

      - name: بررسی فایل APK
        shell: bash
        run: |
          APK_FILES="$(find "$GITHUB_WORKSPACE" -type f -path "*/build/outputs/apk/debug/*.apk")"

          if [ -z "$APK_FILES" ]; then
            echo "::error::Build اجرا شد اما فایل APK پیدا نشد."
            exit 1
          fi

          echo "$APK_FILES"

      - name: قرار دادن APK برای دانلود
        uses: actions/upload-artifact@v7
        with:
          name: IELTS-Leitner-debug-apk
          path: "**/build/outputs/apk/debug/*.apk"
          if-no-files-found: error
          retention-days: 30
